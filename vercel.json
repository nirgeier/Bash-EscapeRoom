{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "build": {
    "env": {
      "PYTHON_VERSION": "3.12"
    }
  },
  "buildCommand": "uv python install 3.12 && uv venv --python 3.12 .venv && . .venv/bin/activate && uv pip install -r mkdocs/requirements.txt && mkdocs build",
  "outputDirectory": "mkdocs-site",
  "installCommand": "echo 'Using Python runtime'",
  "framework": null
}