# Global password configuration for mkdocs-encryptcontent-plugin
# This file defines passwords for individual pages and patterns

# Default password for pages not specified below (optional)
default: "fallback_password"

# Page-specific passwords
pages:
  "rooms/room-02.md": "welldone"
