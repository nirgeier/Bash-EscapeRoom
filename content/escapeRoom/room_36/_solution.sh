#!/usr/bin/env bash
# Room 36 Solution - Signal Trap
# Password: sigcatch

ROOM_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$ROOM_DIR"

echo "=== Room 36: Signal Trap ==="
echo ""

# Use the shortcut script
bash reveal_signal.sh
