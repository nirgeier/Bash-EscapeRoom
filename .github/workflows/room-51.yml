name: Room-51

on:
  push:
    branches:
      - main
    paths:
      - "content/escapeRoom/room_51/**"
      - ".github/workflows/room-51.yml"
  pull_request:
    branches:
      - main
    paths:
      - "content/escapeRoom/room_51/**"
      - ".github/workflows/room-51.yml"
  workflow_dispatch:

jobs:
  test:
    name: "Test Room 51"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq jq rsync xxd openssl netcat-openbsd wget curl bc

      - name: Run Room 51 Solution
        working-directory: content/escapeRoom/room_51
        run: bash _solution.sh
        timeout-minutes: 2
